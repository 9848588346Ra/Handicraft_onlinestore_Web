/**
 * Re-export from infrastructure layer (Clean Architecture).
 * Prefer importing from @/infrastructure/api in new code.
 */
export { api, getToken, setToken, clearToken } from '@/infrastructure/api';
